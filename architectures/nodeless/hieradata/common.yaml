---

# Common classes on all nodes
classes:
  - puppet
  - openssh
  - timezone
  - resolver

# General Settings
timezone::timezone: 'Europe/Rome'
puppet::server: 'puppet01'

resolver::dns_servers:
  - 8.8.8.8
  - 8.8.4.4

